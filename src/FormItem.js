import React from "react";
function FormItem(props) {
	return (
		<div>
			{/* {this.props} */}
			{/* <input name={this.props.fields.name} value={this.props.userFirstName} /> */}
			Where might you be? I can make it longer.
		</div>
	);
}

export default FormItem;
